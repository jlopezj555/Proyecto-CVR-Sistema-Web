{
  "name": "cvr-sistema-web",
  "version": "1.0.0",
  "description": "Sistema Web CVR - Frontend React + Backend Express",
  "main": "backend/server.js",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend ci --silent || npm --prefix frontend install --silent && npm --prefix frontend run build",
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install",
    "build": "npm --prefix frontend run build",
    "dev": "nodemon backend/server.js",
    "start": "node backend/server.js",
    "start:prod": "NODE_ENV=production node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
,
  "keywords": ["cvr", "sistema", "web", "react", "express"],
  "author": "CVR Asesoría",
  "license": "ISC",
  "dependencies": {
    "@mui/material": "^7.3.2",
    "axios": "^1.12.1",
    "bcrypt": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
