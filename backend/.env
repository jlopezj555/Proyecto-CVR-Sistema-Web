# Archivo .env para desarrollo local
# NO subir este archivo a repositorio (agregar a .gitignore)

# Base de datos (puedes usar los valores por defecto que el servidor espera)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=1234
DB_NAME=CVR_LDD
DB_PORT=3306
DB_CONN_LIMIT=10
PORT = 8080

# Alternativa: puedes usar DATABASE_URL en formato mysql://user:pass@host:port/dbname
# DATABASE_URL=mysql://root:1234@localhost:3306/CVR_LDD

# Frontend URL (para CORS y enlaces)
FRONTEND_URL=http://localhost:5173

# JWT secret (cambiar en producción)
JWT_SECRET=secreto_super_seguro

# Email (dejar vacíos para desactivar envío localmente)
EMAIL_USER=impuestos@cvrasesoria.com.gt
EMAIL_PASS=Puesto2025!
EMAIL_FROM="CVR Asesoria <no-reply@localhost>"
EMAIL_HOST=mx.stackmail.com
EMAIL_PORT=465 # 465 para SSL, 587 para TLS/STARTTLS

# Opcional: ajusta según tu entorno
NODE_ENV=development
